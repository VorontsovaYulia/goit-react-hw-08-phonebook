import { styled } from "styled-components";

export const Container = styled.div`
    display: flex;
    gap: 8px;
    flex-wrap: nowrap;
    align-items: center;
    flex-direction: row;
    height: 24px;
`;